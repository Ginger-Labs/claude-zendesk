# Copy to .env and fill in. .env is gitignored.
ZENDESK_SUBDOMAIN=notability
ZENDESK_EMAIL=you@gingerlabs.com
ZENDESK_API_TOKEN=
# Tag that scopes searches to your team's surface. REQUIRED — no default.
# Find candidates with:
#   ./zd get tags.json | jq -r '.tags | sort_by(-.count) | .[0:50] | .[] | "\(.count)\t\(.name)"'
# Common picks: web_app, platform_type_mac, ipad, audio
ZENDESK_TEAM_TAG=
