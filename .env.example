# Copy to .env and fill in. .env is gitignored.
ZENDESK_SUBDOMAIN=notability
ZENDESK_EMAIL=you@gingerlabs.com
ZENDESK_API_TOKEN=
# Tag that scopes searches to your team's surface. Find candidates with:
#   ./zd get tags.json | jq '.tags | sort_by(-.count) | .[0:50]'
ZENDESK_TEAM_TAG=web_app
